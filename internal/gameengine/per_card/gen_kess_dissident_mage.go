package per_card

// registerKessDissidentMage is the auto-generated entry point retained
// so the existing registry call keeps compiling. Real implementation
// lives in custom_kess_dissident_mage.go (once-per-turn cast-from-
// graveyard with exile-on-resolve).
func registerKessDissidentMage(r *Registry) {
	registerKessDissidentMageCustom(r)
}
